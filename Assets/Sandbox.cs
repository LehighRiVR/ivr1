using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Sandbox : MonoBehaviour
{
    /*
    public Image pointer;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    public void CompassPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }

    public void ProgressPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }

    public void ChatPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }

    public void LocationPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }

    public void SettingsPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }

    public void InfoSignPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }

    public void PhotoButtonPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }

    public void VideoButtonPointer()
    {
        pointer.transform.position = transform.position + new Vector3(200, 200);
        pointer.transform.Rotate(0, 0, 180);
    }
    */
}
